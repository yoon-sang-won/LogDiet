# v0.2 Verification

## CLI engine still works

```sh
logdiet wrap -- go test ./...
logdiet raw latest
logdiet grep latest "panic"
```

## Hook rewrite bridge

```sh
logdiet hook rewrite --command "go test ./..."
logdiet hook rewrite --command "vim file.go"
```

Expected:

- `go test ./...` returns `wrap: true`
- `vim file.go` returns `wrap: false`

## Agent setup

```sh
logdiet setup codex --mode rules
logdiet setup codex --mode all
logdiet setup claude --mode rules
logdiet setup cursor --mode rules
logdiet setup gemini --mode rules
logdiet setup antigravity --mode rules
```

## Doctor

```sh
logdiet doctor
```

Doctor should report rules, shims, and native template status without requiring native hooks to be enabled.
