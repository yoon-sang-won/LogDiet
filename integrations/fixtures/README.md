# Adapter Fixtures

These fixtures exercise the central `logdiet hook rewrite` decision path used by native adapter templates.

Run:

```sh
./scripts/verify-adapter-fixtures.sh
```

The script reads `integrations/fixtures/commands.txt` and checks representative wrap and no-wrap decisions.
