# LogDiet for Gemini

This directory contains Gemini instruction and hook templates for LogDiet.

Use `GEMINI.md` as a rules fallback. The hook script is a template unless a tested Gemini hook config is added.
